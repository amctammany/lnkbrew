import DocumentDuplicateIcon from "@heroicons/react/24/outline/DocumentDuplicateIcon";
import { Icon, IconProps } from "./Icon";

export const ForkIcon = (props: IconProps) => (
  <Icon Svg={DocumentDuplicateIcon} {...props} />
);
