import MagnifyingGlassIcon from "@heroicons/react/24/outline/MagnifyingGlassIcon";
import { Icon, IconProps } from "./Icon";

export const SearchIcon = (props: IconProps) => (
  <Icon Svg={MagnifyingGlassIcon} {...props} />
);
