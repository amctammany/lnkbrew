import DocumentIcon from "@heroicons/react/24/outline/DocumentIcon";
import { Icon, IconProps } from "./Icon";

export const SaveIcon = (props: IconProps) => (
  <Icon Svg={DocumentIcon} {...props} />
);
