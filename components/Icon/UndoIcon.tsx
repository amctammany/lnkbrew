import ArrowLeftIcon from "@heroicons/react/24/outline/ArrowLeftIcon";
import { Icon, IconProps } from "./Icon";

export const UndoIcon = (props: IconProps) => (
  <Icon Svg={ArrowLeftIcon} {...props} />
);
