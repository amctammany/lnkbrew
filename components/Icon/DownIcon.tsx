import ArrowDownIcon from "@heroicons/react/24/outline/ArrowDownIcon";
import { Icon, IconProps } from "./Icon";

export const DownIcon = (props: IconProps) => (
  <Icon Svg={ArrowDownIcon} {...props} />
);
